#!/bin/bash
green() {
	echo
	echo -e "\033[1;32m$*\033[0m"
}

clear

green Local status
git status
(( "$?" != 0 )) && exit $?

green Stashes
git stash list --oneline

green Fetching...
git fetch --all

green Incoming changes:
git incoming "$@"

green Outgoing changes:
git outgoing "$@"

echo
